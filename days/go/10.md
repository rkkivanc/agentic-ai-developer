# Day 10: Structs, Methods & Interfaces - Practice

## 📝 Today's Tasks

1.  **Shape Library:** Create interfaces like `Shape` with `Area()` and `Perimeter()`; implement `Circle` and `Rectangle`.
2.  **Logger Abstraction:** Define a `Logger` interface and provide `ConsoleLogger` and `NoopLogger` implementations.
3.  **Manual Test Cases:** Write table-style inputs and expected outputs for your shape methods.
4.  **Code Review:** Review naming, receiver consistency, and interface size in your solutions.

## 📖 Task Explanations

Today consolidates structs, methods, and interfaces through small library-style exercises. The shape example is a classic Go kata that reinforces polymorphism without over-engineering.

The logger abstraction mirrors real service design: depend on interfaces at boundaries, inject concrete implementations later. Take time to review your code as a team would—naming, interface size, and receiver consistency matter.

## 💡 What You'll Gain

By the end of today, you will have designed a small type hierarchy using interfaces and composition, preparing you for error handling and collections.

## 📚 Dictionary for Developers

*   **Abstraction:** Hiding concrete details behind a simpler interface or API contract.
*   **Implementation:** A concrete type that satisfies an interface by providing required methods.
*   **Kata:** A small, focused coding exercise repeated to build fluency.
*   **API Boundary:** The edge where one package or layer exposes functionality to another.
*   **No-op:** An implementation that satisfies an interface but performs no real work, useful for tests or defaults.
