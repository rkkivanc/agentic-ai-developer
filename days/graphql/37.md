# Day 37: Errors, Validation & Nullability - GraphQL Error Format and Extensions

## 📝 Today's Tasks

1.  **Study the Error Object:** Inspect `message`, `locations`, `path`, and `extensions` in error responses.
2.  **Add Custom Extensions:** Attach `code`, `field`, or `status` in resolver-thrown GraphQL errors.
3.  **Separate Error Types:** Distinguish validation, auth, and internal errors via extension codes.
4.  **Client Handling:** Parse extensions in Apollo Client or urql error policies.

## 📖 Task Explanations

GraphQL returns HTTP 200 with partial data and an `errors` array when some fields fail. Extensions carry machine-readable metadata clients use for UI messaging and logging.

Consistent extension codes (`UNAUTHENTICATED`, `BAD_USER_INPUT`) across your API simplify client error handling and observability.

## 💡 What You'll Gain

By the end of today, you will format GraphQL errors with useful extensions and handle them on the client.

## 📚 Dictionary for Developers

*   **GraphQLError:** A spec-defined error object included in the response errors array.
*   **extensions:** Key-value metadata on errors for codes, timestamps, or trace IDs.
*   **Error Path:** The field path where an error occurred in the query tree.
*   **Partial Response:** Successful fields returned alongside errors for other fields.
*   **Error Code:** A stable string in extensions identifying the error category.
