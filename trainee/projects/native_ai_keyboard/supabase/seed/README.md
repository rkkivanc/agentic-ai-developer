# Seed data (optional)

Add SQL or CSV seeds for **local development only**. Do not put production secrets here.

Example use: reference `devices` rows for integration tests after `supabase db reset`.
