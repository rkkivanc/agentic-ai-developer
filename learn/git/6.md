## What Have I Learned?

I learned how `git merge` works. It merges another branch into whichever branch that you are on.
The merge that I tried was a `Fast-forward` merge. It means main branch simply moved forward, there were no new commits on main branch after another branch created.