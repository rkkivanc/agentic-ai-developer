## What Have I Learned?

I learned that `hash` is commit ID.

`git cherry-pick <hash>` re-apply that one commit's changes onto your current branch. New commit with new hash. (not a merge, applies one commit)

You should be on the branch where you want to change.