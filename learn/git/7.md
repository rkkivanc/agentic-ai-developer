## What Have I Learned?

I learned that conflicts happen when both branches change the same part of a file from a shared base. So when the conflict happens we fix file (select things that you want remain, then delete others and markers bla bla), use `git add`, use `git commit` and it commits the fix and completes the merge that we started at the same time.