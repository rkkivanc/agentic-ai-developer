## What Have I Learned?

I learned that `git pull` brings remote updates into your current branch. It works as a download + merge.

`git fetch` only downloads the updates but does not update branch. You should check and merge after that.

If upstream is already set then plain `git pull` will be enough.

